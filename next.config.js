/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost", "www.thecodehelp.in", "www.piecex.com"],
  },
};

module.exports = nextConfig;
